# AIFFEL Campus Online Code Peer Review Template
- 코더 : 이다겸
- 리뷰어 : 정슬기

---

# PRT (Peer Review Template)

- [x] **1. 주어진 문제를 해결하는 완성된 코드가 제출되었나요?**

    루브릭 3가지 모두 완성된 결과물이 제출되었습니다.

    **✅ 루브릭 1 — 전처리 + Augmentation + 3만개 데이터셋**

    MeCab 설치 실패 문제를 `python-mecab-ko`로 대체하여 해결하고,
    사전 훈련된 한국어 Word2Vec 모델(`Kyubyong/wordvectors`)을 활용한
    Lexical Substitution으로 원본 7,737개를 4배 증강하여 **30,956개**를 확보했습니다.

    ```
    30956 30956  ← 루브릭 3만개 기준 충족 ✅
    ```

    **✅ 루브릭 2 — 과적합 방지 하이퍼파라미터**

    Train/Val을 9:1로 분리하여 과적합을 체계적으로 모니터링했습니다.
    60 epoch 기준 Train Loss: 0.65 / Val Loss: 0.85로 격차가 0.2 수준으로 안정적이었습니다.
    Early Stopping + 5 epoch마다 체크포인트 저장으로 최고 모델을 관리했습니다.

    ```python
    # 하이퍼파라미터
    n_layers=2, d_model=368, n_heads=8, d_ff=1024, dropout=0.2
    Warmup Steps=1000, Batch Size=64
    ```

    **✅ 루브릭 3 — 그럴듯한 답변 사례**

    50 epoch 체크포인트 기준 가장 우수한 결과:

    ```
    1. 지루하다, 놀러가고 싶어.            → 같이 놀 러 가 세요.              ✅
    2. 오늘 일찍 일어났더니 피곤하다.       → 언젠간 당신이 좀 더 괜찮아질 거예요. ✅
    3. 간만에 여자친구랑 데이트 하기로 했어. → 좋 은 선택 일 거 예요.           ✅
    4. 집에 있는다는 소리야.               → 제 가 다 들 어 드릴게요.          ✅
    ```

    BLEU Score도 측정하여 정량적 평가까지 완료했습니다.
    ```
    BLEU-1: 0.6422 / BLEU-2: 0.5216 / BLEU-Total: 0.4442 (n=200)
    ```

---

- [x] **2. 전체 코드에서 가장 핵심적이거나 가장 복잡하고 이해하기 어려운 부분에 작성된 주석 또는 doc string을 보고 해당 코드가 잘 이해되었나요?**

    **✅ 핵심 모듈마다 명확한 주석이 달려 있습니다.**

    Transformer 모델 구성 부분이 단계별로 잘 구분되어 있었습니다.

    ```
    Positional Encoding 구현      ✅
    마스크 함수 정의 완료!          ✅
    멀티 헤더 어텐션 정의 완료!     ✅
    FFN 정의 완료!                 ✅
    인코더 레이어 정의 완료!        ✅
    디코더 레이어 정의 완료!        ✅
    Transformer 클래스 정의 완료!   ✅
    ```

    특히 **데이터 증강 부분**에서 기존 함수가 `sentence.split()` 기반이었던 것을
    `que_corpus`가 이미 토큰 리스트라는 점을 인지하고 수정한 이유를 명확히 기록한 점이 좋았습니다.

    ```
    "노드 학습에서 만든 기존 함수는 sentence.split() 문자열을 공백 기준 토큰화 하였다.
    그러나 que_corpus/ans_corpus는 이미 mecab으로 토큰화된 리스트이다.
    따라서 문자열이 아니라 토큰 리스트를 직접 받도록 수정하여 사용한다."
    ```

---

- [x] **3. 에러가 난 부분을 디버깅하여 문제를 해결한 기록을 남겼거나 새로운 시도 또는 추가 실험을 수행해봤나요?**

    **✅ 문제 발생과 해결 과정이 상세히 기록되어 있습니다.**

    **① MeCab 설치 실패 → python-mecab-ko 대체**
    ```python
    # 기존: konlpy.tag.Mecab
    # 대체: from mecab import MeCab
    ```

    **② Checkpoint 미저장으로 best 모델 덮어쓰기 → 5 epoch마다 저장으로 개선** 🌟

    ```
    "안타깝게도 체크포인트를 깜박하여 best 모델을 덮어쓰기를 하는 바람에
    남아있지 않으므로, 이번에는 5 epoch 마다 체크포인트를 저장하도록 한다."
    ```

    실수를 솔직하게 기록하고 즉시 개선한 점이 인상적이었습니다!

    **③ 여러 체크포인트 비교 실험 (20 / 40 / 50 / 55 / 60 epoch)**

    ```
    20 epoch → Loss 계속 하락 중, 추가 학습 결정
    40 epoch → train/val 갭 0.16으로 안정적
    60 epoch → 품질 오히려 저하 확인
    50 epoch → 가장 우수한 번역 품질 확인 ✅
    ```

    **④ BLEU Score 도입으로 정량적 평가 추가**

    **⑤ 사전 훈련된 한국어 Word2Vec 모델 활용** — 단순 랜덤 치환이 아닌
    의미적으로 유사한 단어로 증강하여 데이터 품질을 높인 점이 돋보였습니다.

---

- [x] **4. 회고를 잘 작성했나요?**

    **✅ NOTE 섹션에 깊이 있는 회고가 담겨 있습니다.**

    > *"이번 프로젝트에서 가장 인상 깊었던 부분은 loss와 실제 생성 품질이
    > 항상 비례하지 않는다는 걸 직접 확인한 것이다.
    > 학습을 진행할수록 val loss는 계속 낮아졌지만,
    > 정작 새로운 문장에 대한 답변은 40~50 epoch 구간이
    > 60 epoch보다 오히려 더 자연스러웠다."*

    단순히 코드를 실행한 것이 아니라 **모델의 동작을 직접 체감하고 분석한 결과**입니다.

    또한 BLEU 점수가 높게 나온 이유를 Augmentation 이후 train/val 분리 과정에서의
    **데이터 누수(leakage) 가능성**으로 추론한 부분도 매우 깊은 이해를 보여줍니다.

    ```
    "BLEU 점수 역시 60 epoch에서 더 높게 나왔는데,
    이는 augmentation 이후 train/val을 분리하면서 생긴
    두 세트 간 유사성(leakage) 때문으로 보인다.
    정량 지표 하나만 보고 모델을 고르면 안 된다는 것을 배웠다."
    ```

---

- [x] **5. 코드가 간결하고 효율적인가요?**

    **✅ 전반적으로 모듈화가 잘 되어 있습니다.**

    - `preprocess_sentence()`, `build_corpus()`, `lexical_sub()` 등
      재사용 가능한 함수로 잘 분리되어 있습니다.
    - Train Step / Eval Step을 별도 함수로 분리하여 훈련 루프가 깔끔합니다.
    - 데이터 증강 조합을 4가지 케이스로 명확하게 구분한 점이 가독성이 높습니다.

    ```python
    # 원본 + 3가지 증강 조합으로 4배 확장
    # ① 원본 que + 원본 ans
    # ② 증강 que + 원본 ans
    # ③ 원본 que + 증강 ans
    # ④ 증강 que + 증강 ans
    ```

    **한 가지 아쉬운 점**: 일부 변수명이 짧게 축약되어 (`s`, `d`, `dr` 등)
    처음 보는 사람이 바로 이해하기 어려울 수 있습니다. ( 저같은 사람....... 사실 또 없긴해요 )

---

# 회고 (참고 링크 및 코드 개선)

다겸님의 코드를 리뷰하면서 **"Loss가 낮다고 항상 좋은 모델이 아니다"** 라는 점을 함께 배웠습니다.

특히 인상 깊었던 3가지:

**1. 체크포인트 실수 → 즉시 개선**
실수를 솔직하게 기록하고 바로 5 epoch마다 저장하는 방식으로 개선한 부분이 인상깊었습니다.

**2. 사전 훈련 Word2Vec 활용**
이미 잘 학습된 한국어 모델을 가져와서
더 풍부한 증강을 시도한 점이 새로웠습니다.
(제가 이해한게 맞는지 모르겠습니다.....)

**3. 데이터 누수(leakage) 추론**
BLEU 점수가 높게 나온 원인을 단순히 "잘 됐다"로 끝내지 않고,
증강 데이터의 유사성 문제로 추론한 깊이 있는 분석을 보고 저도 배울점이 또 생겼습니다 !!!

따로 더 제안 드릴바가 없이 회고까지 너무 갓벽 하셨습니다
저도 더 분발하겠습니다. ^ ^ 너무 고생많으셨습니다.
